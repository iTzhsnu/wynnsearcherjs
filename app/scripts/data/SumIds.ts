export type SumIds = {
    sumBaseIds: number[],
    sumAddIds: number[],
    sumMultiIds: number[],
    sumIds: number[],
    isDPS: boolean,
    isMeleeDPS: boolean,
    useAverage: boolean
}

export const sumIds: SumIds[] = [

]